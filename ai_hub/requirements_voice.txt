# ESP32 Voice Streaming Dependencies cho DTHub

# WebSocket client cho ESP32
uwebsockets>=10.0

# Audio processing
SpeechRecognition>=3.10.0
pydub>=0.25.1
gTTS>=2.3.0

# Django Channels (nếu chưa có)
channels>=4.0.0
channels-redis>=4.1.0

# Cho ESP32 MicroPython
# Cần install: uwebsockets

# Install commands:
# pip install SpeechRecognition pydub gTTS
# pip install channels channels-redis
# 
# Cho ESP32:
# mip install uwebsockets
